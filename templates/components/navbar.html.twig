{% set routeActive = app.request.get('_route') %}

<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">Finizens</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link {{ (routeActive == 'home') ? 'active' : '' }}" href="{{ path('home') }}">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ (routeActive == 'list_clients') ? 'active' : '' }}" href="{{ path('list_clients') }}">Clientes</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link {{ (routeActive == 'list_orders') ? 'active' : '' }}" href="{{ path('list_orders') }}">Pedidos</a>
                </li>
            </ul>
        </div>
    </div>
</nav>